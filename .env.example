# File Konfigurasi Grok Imagine API Gateway + Telegram Bot
# Copy file ini menjadi .env lalu isi nilai sesuai kebutuhan.

# ============ Konfigurasi Server ============
HOST=0.0.0.0
PORT=9563
DEBUG=false

# ============ Keamanan API ============
API_KEY=your-api-key

# ============ Konfigurasi Proxy (Opsional) ============
# PROXY_URL=http://127.0.0.1:7890
# PROXY_URL=socks5://127.0.0.1:1080
# HTTP_PROXY=
# HTTPS_PROXY=

# ============ Verifikasi Cloudflare ============
CF_CLEARANCE=

# ============ FlareSolverr (auto CF_CLEARANCE) ============
FLARESOLVERR_URL=http://localhost:8191
CF_REFRESH_INTERVAL=3600

# ============ Konfigurasi SSO ============
SSO_FILE=key.txt

# ============ Penyimpanan Media (Opsional) ============
# BASE_URL=http://your-domain.com
# IMAGES_DIR=data/images
# VIDEOS_DIR=data/videos

# ============ Konfigurasi Generate ============
DEFAULT_ASPECT_RATIO=2:3
GENERATION_TIMEOUT=120

# ============ Konfigurasi Redis (Opsional, direkomendasikan untuk scale) ============
# REDIS_ENABLED=true
# REDIS_URL=redis://localhost:6379/0

# ============ Konfigurasi Rotasi SSO ============
# round_robin | least_used | least_recent | weighted | hybrid
SSO_ROTATION_STRATEGY=hybrid
SSO_DAILY_LIMIT=10

# ============ Konfigurasi Telegram Bot ============
# Bisa juga gunakan BOT_TOKEN atau TG_BOT_TOKEN
TELEGRAM_BOT_TOKEN=
# Bisa juga gunakan ADMIN_ID / ADMIN_IDS / TELEGRAM_ADMIN_IDS
BOT_ADMIN_IDS=
GATEWAY_BASE_URL=http://127.0.0.1:9563
# Jika kosong, bot otomatis fallback ke API_KEY
GATEWAY_API_KEY=

# ============ QRIS Payment Gateway (Hubify) ============
QRIS_API_KEY=
QRIS_WEBHOOK_SECRET=
QRIS_BASE_URL=https://qris.hubify.store/api
QRIS_POLL_INTERVAL=10
QRIS_POLL_TIMEOUT=900

# ============ Konfigurasi Limit Bot (Per User / Harian) ============
# Berlaku untuk user biasa, admin tetap unlimited
USER_DAILY_IMAGE_LIMIT=5
USER_DAILY_VIDEO_LIMIT=1
