# File Konfigurasi GrokPi — Multi-Backend API Gateway + Telegram Bot
# Copy file ini menjadi .env lalu isi nilai sesuai kebutuhan.
# Untuk deploy Docker/Coolify, isi variabel via environment.

# ============ Konfigurasi Server ============
HOST=0.0.0.0
PORT=9563
DEBUG=false

# ============ Keamanan API ============
API_KEY=your-api-key

# ============ Konfigurasi Proxy (Opsional) ============
# PROXY_URL=http://127.0.0.1:7890

# ============ Verifikasi Cloudflare ============
CF_CLEARANCE=

# ============ FlareSolverr (auto CF_CLEARANCE) ============
FLARESOLVERR_URL=http://localhost:8191
CF_REFRESH_INTERVAL=3600

# ============ Konfigurasi SSO Grok ============
SSO_FILE=key.txt
# Untuk Docker/Coolify: pass SSO via env (otomatis ditulis ke key.txt)
# SSO_COOKIE=sso_token_kamu_disini

# ============ Konfigurasi Rotasi SSO ============
SSO_ROTATION_STRATEGY=hybrid
SSO_DAILY_LIMIT=10

# ============ Penyimpanan Media (Opsional) ============
# BASE_URL=http://your-domain.com
# IMAGES_DIR=data/images
# VIDEOS_DIR=data/videos

# ============ Konfigurasi Generate ============
DEFAULT_ASPECT_RATIO=2:3
GENERATION_TIMEOUT=120

# ============ Gemini Business Backend ============
GEMINI_ENABLED=false
# JSON array of accounts (bisa diisi via bot /gemini → Add Server / Auto-Register):
# [{"secure_c_ses":"...","csesidx":"1234","config_id":"uuid-here"}]
GEMINI_ACCOUNTS_CONFIG=

# ============ Gemini Auto-Login & Auto-Register ============
# Domain yang MX record-nya pointing ke generator.email (pisah koma jika >1)
GENERATOR_EMAIL_DOMAINS=
# Headless mode Chromium (true untuk production, false untuk debug)
GEMINI_BROWSER_HEADLESS=true
# Proxy untuk browser automation (opsional)
# GEMINI_AUTH_PROXY=http://proxy:port
# Health check interval (menit)
GEMINI_HEALTH_INTERVAL_MINUTES=15
# Auto-login dead servers saat health check gagal
GEMINI_AUTO_LOGIN_ENABLED=true

# ============ Konfigurasi Redis (Opsional) ============
# REDIS_ENABLED=true
# REDIS_URL=redis://localhost:6379/0

# ============ Konfigurasi Telegram Bot ============
TELEGRAM_BOT_TOKEN=
BOT_ADMIN_IDS=
GATEWAY_BASE_URL=http://127.0.0.1:9563
GATEWAY_API_KEY=

# ============ QRIS Payment Gateway (Hubify) ============
QRIS_API_KEY=
QRIS_WEBHOOK_SECRET=
QRIS_BASE_URL=https://qris.hubify.store/api
QRIS_POLL_INTERVAL=10
QRIS_POLL_TIMEOUT=900

# ============ Konfigurasi Limit Bot (Per User / Harian) ============
USER_DAILY_IMAGE_LIMIT=5
USER_DAILY_VIDEO_LIMIT=1
